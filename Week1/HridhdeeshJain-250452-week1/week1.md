# Week 1 Task


### Comparison & Results

*   **Sobel:** 
    * Image after edge detection is very balanced and it identifies most of the edges. 
    * Abs is used since it has to be converted back to 8 bit pixels as there were negative values also when we were using 64 bit so it was showing error **Unsupported depth image for selected encoder is fallbacked to CV_8U..** 

*   **Scharr:** 
    *   Image after edge detection was very sensitive and it was quite amplified and bright and thick, also it looked quite noisy.
*   **Laplacian:** 
    * Image after edge detection was very Fine and delicate.It found out sharp details .

*  Abs is used since it has to be converted back to 8 bit pixels as there were negative values also when we were using 64 bit so it was showing error **Unsupported depth image for selected encoder is fallbacked to CV_8U..**  



